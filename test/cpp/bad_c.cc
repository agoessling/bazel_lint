#include "bad_c.h"

#include <cstdio>

int main() {
  // This is a very long comment that will need to be wrapped at some point.  I must have forgot the :textwidth
    printf("hellow_World\n");
  printf("special value: %d", value);
    return 1L;
}
